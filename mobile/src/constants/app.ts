/** Bump this when shipping a new mobile build. Keep native versions in sync. */
export const APP_VERSION = '1.0.1';

export const APP_AUTHOR_FIRST = 'farukh';
export const APP_AUTHOR_LAST = 'chenda';
